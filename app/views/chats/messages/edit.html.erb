<%# No-JS fallback for editing (the normal path swaps the bubble inline via
    edit.turbo_stream.erb). %>
<%= chats_styles %>
<div class="chats chats-thread">
  <div class="chats-thread__messages">
    <%= render "chats/messages/edit_form", message: @message %>
  </div>
</div>
