# frozen_string_literal: true

# Intentionally empty: the dummy host pins nothing of its own. The engine's
# pins (controllers/chats/*) are added by Chats::Engine's importmap
# initializer — the importmap integration test asserts they land here.
