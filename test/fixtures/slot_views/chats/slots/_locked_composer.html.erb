<%# Slot fixture prepended only by the test that asserts a host can replace
    the locked composer's BODY while chats keeps the id contract. %>
<p class="dummy-locked-body" data-dummy-slot="locked_composer"><%= notice %> Reopen it from the listing.</p>
